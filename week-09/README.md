# Week 09 — Tools, APIs & Multi-Agent (LangChain)

**Phase 3** · *Tools, APIs & Multi-Agent* · *Source: DeepLearning.AI — [Functions, Tools and Agents with LangChain](https://www.deeplearning.ai/short-courses/functions-tools-agents-langchain/)*

## What I studied

LangChain's abstractions for tool-using agents — `@tool` decorators, `bind_tools`, the AgentExecutor loop, function-calling under the hood, and where the framework adds value vs. where it's pure overhead.

## The takeaway

<!-- NEEDS: 1-2 sentence takeaway, written after actually completing DL6 "Functions, Tools and Agents with LangChain". No work-log evidence this course was done in Week 9 (Apr 9-15) — sessions were walk+podcast; the only Week-9 build was an unrelated Content Curation Strategist agent on OpenHermes/OCI (2026-04-12). Do not fabricate. -->
> *(Pending — see NEEDS comment above.)*

## What surprised me

<!-- NEEDS: the one thing the course undersells, written from real experience with DL6. Pending course completion. -->
> *(Pending — see NEEDS comment above.)*

## Three things I'd tell a colleague

<!-- NEEDS: three concrete, opinionated lessons from doing the LangChain functions/tools/agents work. Pending course completion — do not invent. -->
1. *(Pending)*
2. *(Pending)*
3. *(Pending)*

## Code

<!-- NEEDS (optional): minimal working example in ./code/ — e.g. same tool-using agent in raw Anthropic SDK vs LangChain, with line-count/clarity diff. Omit the section entirely if no code is built. -->
> *(Pending — optional; omit if no code example is built.)*

## Related reading

<!-- NEEDS: 1-2 links that genuinely deepened understanding beyond the course. Pending. -->
- *(Pending — see NEEDS comment above.)*
